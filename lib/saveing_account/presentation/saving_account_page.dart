import 'package:flutter/material.dart';

class SavingAccountPage extends StatelessWidget {
  const SavingAccountPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}